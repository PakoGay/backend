create table if not exists users (
    id bigint generated by default as identity primary key,
    created_at timestamp with time zone not null default now(),
    updated_at timestamp with time zone not null default now(),
    name varchar(120) not null,
    email varchar(180) not null unique,
    password_hash varchar(200) not null,
    role varchar(20) not null,
    audio_enabled boolean not null default true
);

alter table users add column if not exists created_at timestamp with time zone not null default now();
alter table users add column if not exists updated_at timestamp with time zone not null default now();
alter table users add column if not exists name varchar(120);
alter table users add column if not exists email varchar(180);
alter table users add column if not exists password_hash varchar(200);
alter table users add column if not exists role varchar(20);
alter table users add column if not exists audio_enabled boolean not null default true;

create table if not exists refresh_tokens (
    id bigint generated by default as identity primary key,
    created_at timestamp with time zone not null default now(),
    updated_at timestamp with time zone not null default now(),
    user_id bigint not null references users(id) on delete cascade,
    token_hash varchar(128) not null unique,
    expires_at timestamp with time zone not null
);

alter table refresh_tokens add column if not exists created_at timestamp with time zone not null default now();
alter table refresh_tokens add column if not exists updated_at timestamp with time zone not null default now();
alter table refresh_tokens add column if not exists user_id bigint;
alter table refresh_tokens add column if not exists token_hash varchar(128);
alter table refresh_tokens add column if not exists expires_at timestamp with time zone;

create table if not exists units (
    id bigint generated by default as identity primary key,
    created_at timestamp with time zone not null default now(),
    updated_at timestamp with time zone not null default now(),
    title varchar(120) not null,
    description varchar(500) not null,
    sort_order integer not null,
    published boolean not null default false
);

alter table units add column if not exists created_at timestamp with time zone not null default now();
alter table units add column if not exists updated_at timestamp with time zone not null default now();
alter table units add column if not exists title varchar(120);
alter table units add column if not exists description varchar(500);
alter table units add column if not exists sort_order integer;
alter table units add column if not exists published boolean not null default false;

create table if not exists lessons (
    id bigint generated by default as identity primary key,
    created_at timestamp with time zone not null default now(),
    updated_at timestamp with time zone not null default now(),
    unit_id bigint not null references units(id) on delete cascade,
    title varchar(120) not null,
    description varchar(500) not null,
    lesson_type varchar(30) not null,
    difficulty varchar(20) not null,
    base_xp integer not null,
    sort_order integer not null,
    published boolean not null default false
);

alter table lessons add column if not exists created_at timestamp with time zone not null default now();
alter table lessons add column if not exists updated_at timestamp with time zone not null default now();
alter table lessons add column if not exists unit_id bigint;
alter table lessons add column if not exists title varchar(120);
alter table lessons add column if not exists description varchar(500);
alter table lessons add column if not exists lesson_type varchar(30);
alter table lessons add column if not exists difficulty varchar(20);
alter table lessons add column if not exists base_xp integer;
alter table lessons add column if not exists sort_order integer;
alter table lessons add column if not exists published boolean not null default false;

create table if not exists exercises (
    id bigint generated by default as identity primary key,
    created_at timestamp with time zone not null default now(),
    updated_at timestamp with time zone not null default now(),
    lesson_id bigint not null references lessons(id) on delete cascade,
    exercise_type varchar(30) not null,
    prompt varchar(500) not null,
    correct_answer varchar(255) not null,
    display_order integer not null
);

alter table exercises add column if not exists created_at timestamp with time zone not null default now();
alter table exercises add column if not exists updated_at timestamp with time zone not null default now();
alter table exercises add column if not exists lesson_id bigint;
alter table exercises add column if not exists exercise_type varchar(30);
alter table exercises add column if not exists prompt varchar(500);
alter table exercises add column if not exists correct_answer varchar(255);
alter table exercises add column if not exists display_order integer;

create table if not exists children (
    id bigint generated by default as identity primary key,
    created_at timestamp with time zone not null default now(),
    updated_at timestamp with time zone not null default now(),
    parent_id bigint not null references users(id) on delete cascade,
    name varchar(120) not null,
    age integer not null,
    avatar varchar(100) not null,
    starting_level integer not null,
    current_level integer not null default 1,
    xp integer not null default 0,
    daily_streak integer not null default 0,
    progress_percent integer not null default 0,
    last_lesson_completed_on date,
    last_active_at timestamp with time zone
);

alter table children add column if not exists created_at timestamp with time zone not null default now();
alter table children add column if not exists updated_at timestamp with time zone not null default now();
alter table children add column if not exists parent_id bigint;
alter table children add column if not exists name varchar(120);
alter table children add column if not exists age integer;
alter table children add column if not exists avatar varchar(100);
alter table children add column if not exists starting_level integer;
alter table children add column if not exists current_level integer not null default 1;
alter table children add column if not exists xp integer not null default 0;
alter table children add column if not exists daily_streak integer not null default 0;
alter table children add column if not exists progress_percent integer not null default 0;
alter table children add column if not exists last_lesson_completed_on date;
alter table children add column if not exists last_active_at timestamp with time zone;

create table if not exists lesson_completions (
    id bigint generated by default as identity primary key,
    created_at timestamp with time zone not null default now(),
    updated_at timestamp with time zone not null default now(),
    child_id bigint not null references children(id) on delete cascade,
    lesson_id bigint not null references lessons(id) on delete cascade,
    accuracy double precision not null,
    duration_seconds bigint not null,
    stars integer not null,
    xp_earned integer not null,
    completed_at timestamp with time zone not null
);

alter table lesson_completions add column if not exists created_at timestamp with time zone not null default now();
alter table lesson_completions add column if not exists updated_at timestamp with time zone not null default now();
alter table lesson_completions add column if not exists child_id bigint;
alter table lesson_completions add column if not exists lesson_id bigint;
alter table lesson_completions add column if not exists accuracy double precision;
alter table lesson_completions add column if not exists duration_seconds bigint;
alter table lesson_completions add column if not exists stars integer;
alter table lesson_completions add column if not exists xp_earned integer;
alter table lesson_completions add column if not exists completed_at timestamp with time zone;

create table if not exists badge_awards (
    id bigint generated by default as identity primary key,
    created_at timestamp with time zone not null default now(),
    updated_at timestamp with time zone not null default now(),
    child_id bigint not null references children(id) on delete cascade,
    code varchar(60) not null,
    title varchar(120) not null,
    description varchar(255) not null,
    awarded_at timestamp with time zone not null
);

alter table badge_awards add column if not exists created_at timestamp with time zone not null default now();
alter table badge_awards add column if not exists updated_at timestamp with time zone not null default now();
alter table badge_awards add column if not exists child_id bigint;
alter table badge_awards add column if not exists code varchar(60);
alter table badge_awards add column if not exists title varchar(120);
alter table badge_awards add column if not exists description varchar(255);
alter table badge_awards add column if not exists awarded_at timestamp with time zone;

create table if not exists notifications (
    id bigint generated by default as identity primary key,
    created_at timestamp with time zone not null default now(),
    updated_at timestamp with time zone not null default now(),
    parent_id bigint not null references users(id) on delete cascade,
    child_id bigint references children(id) on delete set null,
    type varchar(30) not null,
    title varchar(150) not null,
    message varchar(500) not null,
    is_read boolean not null default false
);

alter table notifications add column if not exists created_at timestamp with time zone not null default now();
alter table notifications add column if not exists updated_at timestamp with time zone not null default now();
alter table notifications add column if not exists parent_id bigint;
alter table notifications add column if not exists child_id bigint;
alter table notifications add column if not exists type varchar(30);
alter table notifications add column if not exists title varchar(150);
alter table notifications add column if not exists message varchar(500);
alter table notifications add column if not exists is_read boolean not null default false;

create table if not exists admin_activity_logs (
    id bigint generated by default as identity primary key,
    created_at timestamp with time zone not null default now(),
    updated_at timestamp with time zone not null default now(),
    admin_id bigint references users(id) on delete set null,
    action varchar(100) not null,
    entity_type varchar(100) not null,
    entity_id bigint not null,
    details varchar(500) not null
);

alter table admin_activity_logs add column if not exists created_at timestamp with time zone not null default now();
alter table admin_activity_logs add column if not exists updated_at timestamp with time zone not null default now();
alter table admin_activity_logs add column if not exists admin_id bigint;
alter table admin_activity_logs add column if not exists action varchar(100);
alter table admin_activity_logs add column if not exists entity_type varchar(100);
alter table admin_activity_logs add column if not exists entity_id bigint;
alter table admin_activity_logs add column if not exists details varchar(500);

create index if not exists idx_children_parent on children(parent_id);
create index if not exists idx_lessons_unit on lessons(unit_id);
create index if not exists idx_exercises_lesson on exercises(lesson_id);
create index if not exists idx_completions_child on lesson_completions(child_id);
create index if not exists idx_notifications_parent on notifications(parent_id);
create index if not exists idx_admin_logs_created on admin_activity_logs(created_at desc);

create unique index if not exists uk_users_email on users(email);
create unique index if not exists uk_refresh_tokens_token_hash on refresh_tokens(token_hash);
create unique index if not exists uk_lesson_completions_child_lesson on lesson_completions(child_id, lesson_id);
create unique index if not exists uk_badge_awards_child_code on badge_awards(child_id, code);
select id, name, email, role from users;
select * from children;
